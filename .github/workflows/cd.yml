name: CD

# Skeleton - the target registry, migration-with-backup step, and deploy
# target aren't decided yet; needs discussion before filling this in fully.

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f docker/Dockerfile -t renot-api:${{ github.sha }} .
      # TODO: log in & push to a container registry (needs a registry/deploy target decision)
      # TODO: run Alembic migrations automatically with a safety check/backup before applying to production
      # TODO: deploy step
